# AutoVend RAG+Agent 端到端系统文档

## 系统概述

AutoVend是一个基于RAG（检索增强生成）和AI Agent的智能汽车销售助手系统。系统通过理解用户需求，从车辆知识库中检索相关信息，并通过Groq大模型生成个性化的销售对话。

## 核心架构

### 用户交互层
```
用户输入 → 系统处理 → 智能响应
```

### 系统处理流程
```
用户查询 → 预处理 → RAG检索 → Agent推理 → 响应生成
```

## 详细流程设计

### 1. 用户输入处理
- **输入类型**: 自然语言文本（中文/英文）
- **预处理**: 文本清洗、意图识别
- **示例输入**:
  - "我想找一辆适合家庭使用的SUV"
  - "预算20万左右，省油一点的轿车"
  - "有什么新能源车推荐吗？"

### 2. RAG检索系统
#### 2.1 数据源
- **车辆数据**: DataInUse/VehicleData/ 目录下的1317个TOML文件
- **数据结构**: 品牌型号、价格区间、技术参数、用户评价等

#### 2.2 向量化处理
```
TOML数据 → 文本提取 → 分块处理 → 向量嵌入 → 存储到向量数据库
```

#### 2.3 语义检索
- **检索策略**: 混合检索（语义+关键词）
- **相关性排序**: 基于相似度和业务规则
- **输出格式**: 结构化车辆信息列表

### 3. Agent智能推理
#### 3.1 对话状态管理
```
WELCOME → PROFILE_ANALYSIS → NEEDS_ANALYSIS → CAR_SELECTION
                                                    ↓ ↑
                                          RESERVATION_4S
                                                    ↓ ↑
                                    RESERVATION_CONFIRMATION → FAREWELL
```

#### 3.2 核心功能
- **需求分析**: 提取用户预算、用途、偏好等关键信息
- **车辆推荐**: 基于检索结果和用户画像生成推荐
- **对话管理**: 维护上下文，处理多轮对话
- **业务逻辑**: 预约试驾、经销商信息等

#### 3.3 Groq集成
- **API调用**: 使用Groq的高性能推理服务
- **模型选择**: 针对不同任务选择合适的模型
- **提示工程**: 优化提示词以获得最佳响应质量

#### 3.4 双层记忆系统与超长对话增量/分层归档压缩
- **双层记忆解耦**：
  - **过程记忆**：`ChatMemoryBuffer` 管理多轮对话 Prompt 上下文（缺省 Token 限制 3000）。
  - **结果记忆**：`StatePatch` 增量日志全量审计记录 `SessionState`（画像/需求/预约）事实变更。
- **增量切片压缩 (Incremental Slicing)**：
  - 按轮次阈值（如每 6 轮）或 **SOP 阶段跳转事件** 动态触发切片归档。
  - **绝不二次压缩**已归档的历史切片，仅对自上次归档以来的新增对话切片生成摘要（`start_turn`-`end_turn`），保持极致低 Token 消耗。
- **高阶分层递归归档 (Hierarchical Aggregation)**：
  - 当归档切片条数达到容量限制（如 `max_slices=4`）时，自动将最旧的多个切片摘要二次融合成一条**高阶全局总结**，确保上下文摘要长度永远处于常数界限内。


### 4. 响应生成
#### 4.1 响应类型
- **信息查询**: 车辆参数、价格、配置等
- **推荐建议**: 基于需求的个性化推荐
- **对话引导**: 引导用户提供更多信息
- **业务办理**: 预约、咨询等

#### 4.2 输出格式
```python
{
    "response_text": "根据您的需求，我推荐几款适合的SUV...",
    "stage": "CAR_SELECTION",
    "recommended_vehicles": [...],
    "next_actions": ["查看详情", "预约试驾"],
    "context_updates": {...}
}
```

## 技术实现细节

### 核心组件
1. **数据加载器** (`src/utils/data_loader.py`)
   - TOML文件解析
   - 数据清洗和标准化
   - 批量处理支持

2. **嵌入模型** (`src/rag/embeddings.py`)
   - 文本向量化
   - 多语言支持
   - 缓存机制

3. **向量存储** (`src/rag/vector_store.py`)
   - 高效检索
   - 增量更新
   - 相似度计算

4. **检索器** (`src/rag/retriever.py`)
   - 查询理解
   - 结果排序
   - 相关性过滤

5. **销售Agent** (`src/agent/sales_agent.py`)
   - 状态机管理
   - 意图识别
   - 响应生成

6. **配置管理** (`src/config.py`)
   - API密钥管理
   - 模型参数配置
   - 环境变量处理

### 数据流示例
```python
# 用户输入
user_input = "我想买一辆30万左右的家用SUV"

# 系统处理
agent = SalesAgent()
result = agent.process(user_input)

# 输出结果
print(result.response_text)
# "根据您的预算和需求，我为您推荐以下几款SUV..."
```

## 系统特性

### 核心优势
1. **智能理解**: 深度理解用户需求和偏好
2. **精准检索**: 基于语义的高效车辆匹配
3. **个性化推荐**: 根据用户画像生成定制化建议
4. **流畅对话**: 自然的多轮对话体验
5. **业务集成**: 完整的销售流程支持

### 性能指标
- **检索延迟**: < 500ms
- **响应生成**: < 2s
- **准确率**: > 85%
- **用户满意度**: > 90%

## 使用场景

### 典型对话流程
```
用户: 你好，我想买辆车

系统: 您好！我是AutoVend智能销售助手。为了给您推荐最合适的车型，
      请告诉我您的预算范围和用车需求？

用户: 预算30万左右，主要家用，偶尔长途

系统: 了解您的需求。基于30万预算和家用需求，我为您推荐几款SUV：
      1. 汉兰达 - 空间大，可靠性高
      2. 理想ONE - 新能源，智能化程度高
      3. 昂科威Plus - 舒适性好，性价比高
      您对哪款车比较感兴趣？

用户: 想了解一下汉兰达

系统: 汉兰达是丰田旗下的中型SUV，主要优势：
      • 空间：7座布局，后备箱容积大
      • 动力：2.0T+6AT，平顺可靠
      • 油耗：综合8.5L/100km
      • 价格：25.88-34.88万
      您是否需要预约试驾？
```

## 部署和运维

### 环境要求
- Python 3.11+
- uv包管理器
- Groq API访问权限
- 足够的存储空间（向量数据）

### 配置步骤
1. 安装依赖: `uv sync`
2. 配置API密钥: 复制`.env.example`到`.env`
3. 构建向量索引: `python scripts/build_index.py`
4. 运行系统: `python src/main.py`

### 监控指标
- API调用成功率
- 检索响应时间
- 用户对话质量
- 系统资源使用

## 未来扩展

### 功能规划
1. **多模态支持**: 图片、语音输入
2. **个性化学习**: 用户行为分析
3. **实时数据**: 价格、库存同步
4. **多语言**: 扩展更多语言支持
5. **移动端**: 开发移动应用

### 技术优化
1. **模型微调**: 针对汽车销售领域优化
2. **缓存策略**: 提升响应速度
3. **分布式**: 支持高并发访问
4. **安全增强**: 数据隐私保护

---

*本文档描述了AutoVend RAG+Agent系统的完整端到端设计，为开发和部署提供全面指导。*
